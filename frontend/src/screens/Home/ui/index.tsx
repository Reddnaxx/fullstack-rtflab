import { Text } from '@/shared/ui';

import type { FC } from 'react';

export const HomePage: FC = () => {
  return (
    <div>
      <Text as="h2">Home page</Text>
    </div>
  );
};

export default HomePage;
